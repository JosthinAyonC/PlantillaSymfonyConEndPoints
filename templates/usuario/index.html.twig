{% extends 'base.html.twig' %}

{% block title %}Usuario index{% endblock %}

{% block body %}
<br>
<div class="container-fluid">
    <div class="bg-secondary rounded h-100 p-4">
        <div class="bg-secondary rounded h-100 p-4">
            <h6 class="mb-4">Usuarios Table</h6>
            <table class="table" id="tablausuarios">
                <thead>
                    <tr>
                        <th>IdUsuario</th>
                        <th>Nombre</th>
                        <th>Correo</th>
                        <th>Rol</th>
                        <th>Estado</th>
                        {% if is_granted('ROLE_ADMIN') %}
                        <th>Acciones</th>
                        {% else %}
                        <th>Acciones *BLOQUEADO*</th>
                        {% endif %}
                    </tr>
                </thead>
                <tbody>
                    
                </tbody>

            </table>
            {% if is_granted('ROLE_ADMIN') %}
            <a class="btn btn-primary m-2" href="{{path('app_usuario_nuevo') }}">Crear nuevo usuario</a>
            {% endif %}
        </div>
    </div>
</div>

<script src="{{asset('js/scriptUsuario.js')}}"></script>

{% endblock %}